<Window x:Class="PointRead.FloatingToolbarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="432"
        Height="64"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Padding="10"
            Background="#FAFBFD"
            BorderBrush="#C7D4E4"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Width="28"
                    Height="28"
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    CornerRadius="8"
                    Background="#2563EB">
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="SemiBold"
                           Text="P" />
            </Border>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Button Width="56"
                        Margin="0,0,6,0"
                        Click="StartSelectionButton_Click"
                        Content="框读" />
                <Button Width="68"
                        Margin="0,0,6,0"
                        Click="ClipboardReadButton_Click"
                        Content="剪贴板" />
                <Button Width="42"
                        Margin="0,0,6,0"
                        Click="PreviousButton_Click"
                        Content="-5s" />
                <Button Width="58"
                        Margin="0,0,6,0"
                        Click="PlayPauseButton_Click"
                        Content="暂停" />
                <Button Width="42"
                        Margin="0,0,6,0"
                        Click="NextButton_Click"
                        Content="+5s" />
                <Button Width="50"
                        Click="StopButton_Click"
                        Content="停止" />
            </StackPanel>
            <Button Grid.Column="2"
                    Width="28"
                    Height="28"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Click="HideButton_Click"
                    Content="×" />
        </Grid>
    </Border>
</Window>
